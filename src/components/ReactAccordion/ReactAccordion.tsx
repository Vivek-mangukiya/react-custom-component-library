import { FunctionComponent } from "react";
import { ReactAccordionProps } from "./ReactAccordion.type";

export const ReactAccordion: FunctionComponent<ReactAccordionProps> = ({}) => {
  return <></>;
};
